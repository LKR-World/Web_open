# Web_open

Eine kleine statische Test-Webseite aus HTML, CSS und JavaScript – kein Build-Tool nötig.

## Ausführen

Einfach `index.html` im Browser öffnen, oder lokal servieren:

```bash
python3 -m http.server
```

Danach im Browser `http://localhost:8000` aufrufen.
